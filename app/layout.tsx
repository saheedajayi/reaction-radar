import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "ReactionRadar - Know What the Internet Thinks in Real-Time",
  description:
    "Analyze public sentiment and reactions across social media platforms in real-time with AI-powered insights.",
  keywords: "sentiment analysis, social media monitoring, AI, real-time analytics",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
